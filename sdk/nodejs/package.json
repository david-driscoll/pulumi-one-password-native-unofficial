{
    "name": "@pulumi/onepassword",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource onepassword ${VERSION}"
    },
    "dependencies": {
        "@pulumi/pulumi": "latest"
    },
    "devDependencies": {
        "typescript": "^5.2.2"
    },
    "pulumi": {
        "resource": true
    }
}
